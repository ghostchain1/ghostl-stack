# GhostStack mainnet aggregate compose bundle
#
# Deprecated placeholder only.
# This file no longer defines the canonical production runtime and must not be
# used for GhostChain / GhostL2 / GhostL3 deployment.
#
# Canonical production ownership is split across the active Ghost-native
# compose files and host-level network infrastructure:
#   - infra/ghostchain/docker-compose.l1.yml
#   - docker-compose.custom-rollup.yml
#   - docker-compose.phase3.yml
#   - observability/infra/docker-compose.yml
#   - docker-compose.sovereign.yml
#   - apps/api-compose.yml
#   - docker-compose.ghostbrain.yml
#   - infra/network/nginx/nginx.conf
#   - infra/network/kong/kong.yaml
#   - infra/network/haproxy/haproxy-geo.cfg
#
# This stub is intentionally empty so stale automation fails closed instead of
# booting an obsolete OP-shaped mainnet bundle.

name: ghostl-mainnet-deprecated
services: {}
