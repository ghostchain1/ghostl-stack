# GhostStack Docker IP-binding overlay
#
# Deprecated placeholder only.
# The old container-level IP binding overlay has been retired from the canonical
# production path because it was coupled to deleted OP-era service names.
#
# Canonical network publication is now host-level and lives in:
#   - infra/network/nginx/nginx.conf
#   - infra/network/kong/kong.yaml
#   - infra/network/haproxy/haproxy-geo.cfg
#   - infra/network/firewall/ufw-setup.sh
#
# Canonical chain RPC endpoints remain:
#   - GhostChain L1 : 18545
#   - GhostL2       : 29547
#   - GhostL3       : 39545
#
# This stub remains only so legacy references resolve to an inert file instead
# of a stale OP-shaped overlay.

name: ghoststack-network-overlay-deprecated
services: {}
