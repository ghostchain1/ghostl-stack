# Private keys and secrets (do not commit the real file).
# Copy to .env.secrets (gitignored) and fill with real values.

SEQUENCER_KEY=REPLACE_ME
BATCHER_KEY=REPLACE_ME
PROPOSER_KEY=REPLACE_ME
CHALLENGER_KEY=REPLACE_ME
L3_CHALLENGER_KEY=REPLACE_ME
