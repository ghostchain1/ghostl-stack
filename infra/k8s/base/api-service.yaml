apiVersion: v1
kind: Service
metadata:
  name: ghostl-api
  namespace: ghostl
spec:
  selector:
    app: ghostl-api
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
