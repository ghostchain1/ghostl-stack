apiVersion: v1
kind: Service
metadata:
  name: ghostl-web
  namespace: ghostl
spec:
  selector:
    app: ghostl-web
  ports:
    - port: 3200
      targetPort: 3200
      protocol: TCP
      name: http
