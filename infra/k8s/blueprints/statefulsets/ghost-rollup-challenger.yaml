{
  "apiVersion": "apps/v1",
  "kind": "StatefulSet",
  "metadata": {
    "name": "ghost-rollup-challenger",
    "labels": {
      "app": "ghost-rollup-challenger",
      "role": "chain"
    },
    "annotations": {
      "ghost.source.compose": "/home/ghost/ghostl-stack/infra/docker/compose/docker-compose.services.yml",
      "ghost.no_recreate": "true"
    }
  },
  "spec": {
    "serviceName": "ghost-rollup-challenger",
    "replicas": 1,
    "podManagementPolicy": "OrderedReady",
    "updateStrategy": {
      "type": "OnDelete"
    },
    "selector": {
      "matchLabels": {
        "app": "ghost-rollup-challenger"
      }
    },
    "template": {
      "metadata": {
        "labels": {
          "app": "ghost-rollup-challenger",
          "role": "chain"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "ghost-rollup-challenger",
            "image": "ghostl/ghost-rollup-challenger:local",
            "command": null,
            "args": null,
            "env": [
              {
                "name": "PORT",
                "value": "7282"
              },
              {
                "name": "RPC_SETTLEMENT",
                "value": "${RPC_L2}"
              },
              {
                "name": "RPC_CHILD",
                "value": "${RPC_L3}"
              },
              {
                "name": "ROLLUP_ADDRESS",
                "value": "${L2_ROLLUP_L3_ADDRESS}"
              },
              {
                "name": "CHALLENGER_PRIVATE_KEY",
                "value": "${CHALLENGER_PRIVATE_KEY}"
              },
              {
                "name": "STATE_DIR",
                "value": "/state"
              }
            ],
            "ports": [
              {
                "containerPort": 7282,
                "protocol": "TCP"
              }
            ],
            "volumeMounts": [
              {
                "name": "./data",
                "mountPath": "/state"
              }
            ]
          }
        ]
      }
    },
    "volumeClaimTemplates": [
      {
        "metadata": {
          "name": "./data"
        },
        "spec": {
          "accessModes": [
            "ReadWriteOnce"
          ],
          "resources": {
            "requests": {
              "storage": "10Gi"
            }
          }
        }
      }
    ]
  }
}
