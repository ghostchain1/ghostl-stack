{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": {
    "name": "global-search-service",
    "labels": {
      "app": "global-search-service",
      "role": "api"
    },
    "annotations": {
      "ghost.source.compose": "/home/ghost/ghostl-stack/infra/docker/compose/docker-compose.services.yml",
      "ghost.no_recreate": "false"
    }
  },
  "spec": {
    "replicas": 1,
    "selector": {
      "matchLabels": {
        "app": "global-search-service"
      }
    },
    "template": {
      "metadata": {
        "labels": {
          "app": "global-search-service",
          "role": "api"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "global-search-service",
            "image": "ghostl/global-search-service:local",
            "command": null,
            "args": null,
            "env": [
              {
                "name": "PORT",
                "value": "7637"
              }
            ],
            "ports": [
              {
                "containerPort": 7637,
                "protocol": "TCP"
              }
            ],
            "volumeMounts": []
          }
        ]
      }
    }
  }
}
