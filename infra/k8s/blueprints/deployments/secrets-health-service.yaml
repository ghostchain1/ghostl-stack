{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": {
    "name": "secrets-health-service",
    "labels": {
      "app": "secrets-health-service",
      "role": "api"
    },
    "annotations": {
      "ghost.source.compose": "/home/ghost/ghostl-stack/infra/docker/compose/docker-compose.services.yml",
      "ghost.no_recreate": "false"
    }
  },
  "spec": {
    "replicas": 1,
    "selector": {
      "matchLabels": {
        "app": "secrets-health-service"
      }
    },
    "template": {
      "metadata": {
        "labels": {
          "app": "secrets-health-service",
          "role": "api"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "secrets-health-service",
            "image": "ghostl/secrets-health-service:local",
            "command": null,
            "args": null,
            "env": [
              {
                "name": "PORT",
                "value": "7618"
              },
              {
                "name": "PROM_URL",
                "value": "${PROM_URL}"
              }
            ],
            "ports": [
              {
                "containerPort": 7618,
                "protocol": "TCP"
              }
            ],
            "volumeMounts": []
          }
        ]
      }
    }
  }
}
