{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": {
    "name": "treasury-service",
    "labels": {
      "app": "treasury-service",
      "role": "api"
    },
    "annotations": {
      "ghost.source.compose": "/home/ghost/ghostl-stack/infra/docker/compose/docker-compose.core.yml",
      "ghost.no_recreate": "false"
    }
  },
  "spec": {
    "replicas": 1,
    "selector": {
      "matchLabels": {
        "app": "treasury-service"
      }
    },
    "template": {
      "metadata": {
        "labels": {
          "app": "treasury-service",
          "role": "api"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "treasury-service",
            "image": "local/treasury-service:${OPSTACK_IMAGE_TAG:-devnet}",
            "command": null,
            "args": null,
            "env": [
              {
                "name": "PROM_URL",
                "value": "${PROM_URL:-http://prometheus:9090}"
              }
            ],
            "ports": [],
            "volumeMounts": []
          }
        ]
      }
    }
  }
}
