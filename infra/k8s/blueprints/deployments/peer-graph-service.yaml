{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": {
    "name": "peer-graph-service",
    "labels": {
      "app": "peer-graph-service",
      "role": "api"
    },
    "annotations": {
      "ghost.source.compose": "/home/ghost/ghostl-stack/infra/docker/compose/docker-compose.services.yml",
      "ghost.no_recreate": "false"
    }
  },
  "spec": {
    "replicas": 1,
    "selector": {
      "matchLabels": {
        "app": "peer-graph-service"
      }
    },
    "template": {
      "metadata": {
        "labels": {
          "app": "peer-graph-service",
          "role": "api"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "peer-graph-service",
            "image": "ghostl/peer-graph-service:local",
            "command": null,
            "args": null,
            "env": [
              {
                "name": "PORT",
                "value": "7636"
              },
              {
                "name": "RPC_L2",
                "value": "${RPC_L2}"
              },
              {
                "name": "RPC_L3",
                "value": "${RPC_L3}"
              }
            ],
            "ports": [
              {
                "containerPort": 7636,
                "protocol": "TCP"
              }
            ],
            "volumeMounts": []
          }
        ]
      }
    }
  }
}
