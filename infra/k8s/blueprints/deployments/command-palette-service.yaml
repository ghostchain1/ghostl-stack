{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": {
    "name": "command-palette-service",
    "labels": {
      "app": "command-palette-service",
      "role": "api"
    },
    "annotations": {
      "ghost.source.compose": "/home/ghost/ghostl-stack/infra/docker/compose/docker-compose.services.yml",
      "ghost.no_recreate": "false"
    }
  },
  "spec": {
    "replicas": 1,
    "selector": {
      "matchLabels": {
        "app": "command-palette-service"
      }
    },
    "template": {
      "metadata": {
        "labels": {
          "app": "command-palette-service",
          "role": "api"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "command-palette-service",
            "image": "ghostl/command-palette-service:local",
            "command": null,
            "args": null,
            "env": [
              {
                "name": "PORT",
                "value": "7642"
              }
            ],
            "ports": [
              {
                "containerPort": 7642,
                "protocol": "TCP"
              }
            ],
            "volumeMounts": []
          }
        ]
      }
    }
  }
}
