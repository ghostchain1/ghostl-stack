{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": {
    "name": "block-index-service",
    "labels": {
      "app": "block-index-service",
      "role": "api"
    },
    "annotations": {
      "ghost.source.compose": "/home/ghost/ghostl-stack/infra/docker/compose/docker-compose.services.yml",
      "ghost.no_recreate": "false"
    }
  },
  "spec": {
    "replicas": 1,
    "selector": {
      "matchLabels": {
        "app": "block-index-service"
      }
    },
    "template": {
      "metadata": {
        "labels": {
          "app": "block-index-service",
          "role": "api"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "block-index-service",
            "image": "ghostl/block-index-service:local",
            "command": null,
            "args": null,
            "env": [
              {
                "name": "PORT",
                "value": "7626"
              },
              {
                "name": "PROM_URL",
                "value": "${PROM_URL}"
              }
            ],
            "ports": [
              {
                "containerPort": 7626,
                "protocol": "TCP"
              }
            ],
            "volumeMounts": []
          }
        ]
      }
    }
  }
}
