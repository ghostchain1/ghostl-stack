{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": {
    "name": "explainability-service",
    "labels": {
      "app": "explainability-service",
      "role": "ai"
    },
    "annotations": {
      "ghost.source.compose": "/home/ghost/ghostl-stack/infra/docker/compose/docker-compose.ai.yml",
      "ghost.no_recreate": "false"
    }
  },
  "spec": {
    "replicas": 1,
    "selector": {
      "matchLabels": {
        "app": "explainability-service"
      }
    },
    "template": {
      "metadata": {
        "labels": {
          "app": "explainability-service",
          "role": "ai"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "explainability-service",
            "image": "ghostl/explainability-service:local",
            "command": null,
            "args": null,
            "env": [
              {
                "name": "PORT",
                "value": "7632"
              },
              {
                "name": "PROM_URL",
                "value": "${PROM_URL}"
              }
            ],
            "ports": [
              {
                "containerPort": 7632,
                "protocol": "TCP"
              }
            ],
            "volumeMounts": []
          }
        ]
      }
    }
  }
}
