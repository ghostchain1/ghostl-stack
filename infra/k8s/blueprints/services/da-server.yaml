{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "da-server",
    "labels": {
      "app": "da-server"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "da-server"
    },
    "ports": [
      {
        "port": "3100",
        "targetPort": 3100,
        "protocol": "TCP"
      }
    ]
  }
}
