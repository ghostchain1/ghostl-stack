{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "tx-index-service",
    "labels": {
      "app": "tx-index-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "tx-index-service"
    },
    "ports": [
      {
        "port": "7625",
        "targetPort": 7625,
        "protocol": "TCP"
      }
    ]
  }
}
