{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "bridge-service",
    "labels": {
      "app": "bridge-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "bridge-service"
    },
    "ports": [
      {
        "port": "7604",
        "targetPort": 7604,
        "protocol": "TCP"
      }
    ]
  }
}
