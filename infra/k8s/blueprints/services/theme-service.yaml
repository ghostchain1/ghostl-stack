{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "theme-service",
    "labels": {
      "app": "theme-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "theme-service"
    },
    "ports": [
      {
        "port": "7634",
        "targetPort": 7634,
        "protocol": "TCP"
      }
    ]
  }
}
