{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "dispute-service",
    "labels": {
      "app": "dispute-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "dispute-service"
    },
    "ports": [
      {
        "port": "7607",
        "targetPort": 7607,
        "protocol": "TCP"
      }
    ]
  }
}
