{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "command-palette-service",
    "labels": {
      "app": "command-palette-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "command-palette-service"
    },
    "ports": [
      {
        "port": "7642",
        "targetPort": 7642,
        "protocol": "TCP"
      }
    ]
  }
}
