{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "validator-service",
    "labels": {
      "app": "validator-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "validator-service"
    },
    "ports": [
      {
        "port": "7600",
        "targetPort": 7600,
        "protocol": "TCP"
      }
    ]
  }
}
