{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "proxy-inspector-service",
    "labels": {
      "app": "proxy-inspector-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "proxy-inspector-service"
    },
    "ports": [
      {
        "port": "7631",
        "targetPort": 7631,
        "protocol": "TCP"
      }
    ]
  }
}
