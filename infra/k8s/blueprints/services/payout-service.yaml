{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "payout-service",
    "labels": {
      "app": "payout-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "payout-service"
    },
    "ports": [
      {
        "port": "7629",
        "targetPort": 7629,
        "protocol": "TCP"
      }
    ]
  }
}
