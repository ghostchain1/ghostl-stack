{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "session-service",
    "labels": {
      "app": "session-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "session-service"
    },
    "ports": [
      {
        "port": "7643",
        "targetPort": 7643,
        "protocol": "TCP"
      }
    ]
  }
}
