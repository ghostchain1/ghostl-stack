{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "peer-graph-service",
    "labels": {
      "app": "peer-graph-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "peer-graph-service"
    },
    "ports": [
      {
        "port": "7636",
        "targetPort": 7636,
        "protocol": "TCP"
      }
    ]
  }
}
