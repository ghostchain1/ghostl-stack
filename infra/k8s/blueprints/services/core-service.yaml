{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "core-service",
    "labels": {
      "app": "core-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "core-service"
    },
    "ports": [
      {
        "port": "8080",
        "targetPort": 8080,
        "protocol": "TCP"
      }
    ]
  }
}
