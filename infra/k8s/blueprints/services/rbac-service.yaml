{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "rbac-service",
    "labels": {
      "app": "rbac-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "rbac-service"
    },
    "ports": [
      {
        "port": "7640",
        "targetPort": 7640,
        "protocol": "TCP"
      }
    ]
  }
}
