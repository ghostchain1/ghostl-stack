{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "contract-registry-service",
    "labels": {
      "app": "contract-registry-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "contract-registry-service"
    },
    "ports": [
      {
        "port": "7608",
        "targetPort": 7608,
        "protocol": "TCP"
      }
    ]
  }
}
