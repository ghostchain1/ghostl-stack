{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "feature-flags-service",
    "labels": {
      "app": "feature-flags-service"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "feature-flags-service"
    },
    "ports": [
      {
        "port": "7611",
        "targetPort": 7611,
        "protocol": "TCP"
      }
    ]
  }
}
