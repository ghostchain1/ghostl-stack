{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "ghostscout",
    "labels": {
      "app": "ghostscout"
    }
  },
  "spec": {
    "type": "NodePort",
    "selector": {
      "app": "ghostscout"
    },
    "ports": [
      {
        "port": "18644",
        "targetPort": 4000,
        "protocol": "TCP"
      }
    ]
  }
}
