fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
prometheus-client>=0.20.0
docker>=7.1.0
psutil>=6.0.0
# libvirt-python is optional — install separately when libvirt is available on the host.
# Graceful degradation: VM listing returns [] when not installed.
# libvirt-python>=10.0.0
