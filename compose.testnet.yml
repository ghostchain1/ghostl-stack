# GhostStack testnet aggregate compose bundle
#
# Deprecated placeholder only.
# This file no longer owns any canonical GhostChain / GhostL2 / GhostL3 runtime
# services and must not be used for deployment promotion.
#
# Canonical testnet bring-up now lives in:
#   - infra/ghostchain/docker-compose.l1.yml
#   - docker-compose.custom-rollup.yml
#   - docker-compose.phase3.yml
#   - observability/infra/docker-compose.yml
#   - docker-compose.sovereign.yml
#   - apps/api-compose.yml
#   - docker-compose.ghostbrain.yml
#   - scripts/testnet/00-preflight.sh
#   - scripts/testnet/20-up.sh
#   - genesis-installer/start_stack.sh
#
# This stub is intentionally empty so stale automation fails closed instead of
# silently launching an obsolete OP-shaped topology.

name: ghostl-testnet-deprecated
services: {}
