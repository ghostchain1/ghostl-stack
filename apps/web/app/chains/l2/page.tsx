import { ChainLayerDashboard } from '../../../src/modules/chain/components/ChainLayerDashboard';

export const metadata = { title: 'GhostL2 — GhostStack' };

export default function L2Page() {
  return <ChainLayerDashboard layer="l2" />;
}
