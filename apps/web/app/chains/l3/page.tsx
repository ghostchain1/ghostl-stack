import { ChainLayerDashboard } from '../../../src/modules/chain/components/ChainLayerDashboard';

export const metadata = { title: 'GhostL3 — GhostStack' };

export default function L3Page() {
  return <ChainLayerDashboard layer="l3" />;
}
