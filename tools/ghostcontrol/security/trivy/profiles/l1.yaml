name: ghostcontrol-l1
target: /home/ghost/ghostl-stack/infra/ghostchain
scanner:
  mode: fs
  scanners:
    - vuln
    - misconfig
    - secret
  severity:
    - HIGH
    - CRITICAL
  ignore_unfixed: true
output:
  format: json
  file: /home/ghost/ghostl-stack/tools/ghostcontrol/evidence/scans/trivy-l1.json

