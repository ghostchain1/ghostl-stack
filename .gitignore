.DS_Store
.vscode/
.idea/

# dependencies
**/node_modules/

# local-only env variants
.env.local
.env.*.local
/.env
**/.env
**/.env.local
**/.env.*.local
.env.*
**/.env.*
!.env.example
!.env.*.example
!.env.sample
!.env.*.sample
# compliance local env
services/ghost-compliance/.env
services/ghost-compliance-worker/.env
apps/ghost-ui/.env
infra/opstack/.env
infra/opstack/.env.local
infra/opstack/.env.secrets
infra/opstack/.env.*.local
services/stack.env
services/.env
# root-level stack.env may contain real secrets — never track
/stack.env

# environment-specific env files (non-dotfile naming, e.g. ghostchain.env)
environments/**/*.env
!environments/**/*.env.example
!environments/**/*.env.sample

# docker compose runtime env files (non-dotfile naming)
infra/docker/compose/*.env
infra/docker/compose/**/*.env
!infra/docker/compose/*.env.example
!infra/docker/compose/**/*.env.example

# hypervisor env files (non-dotfile naming, e.g. ghoststack.env)
infra/hypervisor/**/*.env
!infra/hypervisor/**/*.env.example
!infra/hypervisor/**/*.env.sample

# observability / infra env files
observability/**/.env
observability/**/stack.env

# vault rendered secrets (local only)
infra/opstack/secrets/
infra/opstack/l3/secrets/
infra/ghostchain/secrets/

# local docker secrets (Phase 3)
infra/docker/secrets/

# generated by contracts build/deploy
contracts/artifacts/
contracts/cache/
contracts/.hardhat-cache/
contracts/typechain-types/
contracts/proposals/

# avoid committing build caches and forge artifacts
cache/
out/

# generated at runtime by deploy script
services/ghost-guard/.env
services/ghost-relayer/.env
services/ghost-rollup-proposer/.env.l2
services/ghost-rollup-proposer/.env.l3
services/ghost-rollup-challenger/.env.l2
services/ghost-rollup-challenger/.env.l3

# local scratch
.tmp/
.gomodcache/

# local releases (generated)
releases/local-*/

# python bytecode
**/__pycache__/
*.pyc

# forge broadcast artifacts
infra/opstack/broadcast/

# polygon-edge chain data (generated)
chains/**/data/

# local infra node data (permission restricted)
infra/opstack/data/
infra/opstack/l3/ghostsample/data/
infra/opstack/data/l2-geth/
infra/opstack/data/l1-geth/
infra/opstack/data/l2-geth-new/
infra/opstack/data/l2-geth-new2/
infra/opstack/data/l1-geth-new/
infra/opstack/data/op-node/
infra/opstack/data/op-sequencer/
infra/opstack/data/guard/
infra/opstack/data/l2-geth-*/
infra/opstack/l3/ghostl3/data/
infra/opstack/l3/ghostl3/data-*/
infra/opstack/l3/**/backups-*/
/infra/opstack/optimism/
infra/opstack/infra/opstack/optimism/
apps/api/data/
apps/api/.sessions/
infra/ghostchain/data/
infra/ghostchain/logs/
infra/ghostchain/config/bootnode-enode.txt
infra/ghostchain/ghostl2/data/
infra/ghostchain/**/logs/
# geth runtime data lives under infra/ghostchain/data (keep infra/ghostchain/geth templates tracked)
infra/ghostchain/data/**/geth/
infra/ghostchain/data/**/keystore/

# runtime state snapshots
services/data/state.json
services/**/data/
!services/l3-fee-collector/data/
!services/l3-fee-collector/data/.gitkeep
!services/l2-revenue-aggregator/data/
!services/l2-revenue-aggregator/data/.gitkeep
!services/treasury-engine/data/
!services/treasury-engine/data/.gitkeep
!services/reward-distributor/data/
!services/reward-distributor/data/.gitkeep
!services/hyper-ghost-governor/data/
!services/hyper-ghost-governor/data/.gitkeep
services/.state/

# build artifacts
apps/web/.next/
**/.next/
**/.next-ghost/
**/.next-codex/
**/.next-local-*/
apps/web/.next-codex/
apps/web/.next-ghost/
apps/web/.next-local-*/
dist/
**/dist/

.git/gc.log

# logs (runtime)
*.log
/logs/
infra/**/logs/
services/**/logs/

# coverage
coverage/
**/coverage/

# Flutter / Dart build artifacts
**/.dart_tool/
**/.flutter-plugins
**/.flutter-plugins-dependencies
**/build/
packages/ghost_flutter_sdk/.dart_tool/
packages/ghost_flutter_sdk/build/

# SQLite runtime databases (litvyblive backend)
apps/litvyblive/backend/*.db
apps/litvyblive/backend/*.db-shm
apps/litvyblive/backend/*.db-wal

# Next.js public auto-generated assets
apps/web-apps/public/
apps/web-company/public/
apps/web-dev/public/
apps/web-exchange/public/
apps/web-explorer/public/
apps/web-governance/public/
apps/web-investor/public/
apps/web-main/public/
apps/web-nodes/public/
apps/web-portal/public/
apps/web-status/public/
.nyc_output/

# tooling caches
.turbo/
**/.turbo/
.parcel-cache/
**/.parcel-cache/
.vite/
**/.vite/
.eslintcache
.stylelintcache
.cache/
**/.cache/

# common JS tool logs/cache
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo

# ops snapshots + attestations (generated by ghostctl)
ops/docker/snapshots/*
!ops/docker/snapshots/.gitkeep
!ops/docker/snapshots/README.md
ops/docker/attestations/*
!ops/docker/attestations/.gitkeep
!ops/docker/attestations/README.md
ops/snapshots/
ops/preflight/
ops/security/trivy-fs.json
trivy-fs.json
ops/security/**/status.json
ops/ai/anomaly/anomaly-report.json
ops/ai/anomaly/model-metadata.json
ops/ai/drift/baseline.json
ops/ai/drift/drift-report.json
ops/confidential/cca.json
ops/onchain/notarization.json
ops/onchain/cross-chain-anchors.json
ops/mev/mev-report.json
ops/zk/immutability-proof.json
ops/zk/immutability-proof.verifier.json
ops/zk/immutability-proof.onchain.json
ops/zk/build/
ops/zk/Verifier.sol
ops/quorum/quorum-attestation.json
ops/quorum/regions/*.json
ops/ai/threat-model/stride-model.md
ops/ai/threat-model/linddun-model.md
ops/ai/threat-model/risk-summary.json
infra/evidence/out/
ops/geo-risk/quorum-selection.json
ops/compliance/evidence-bundle.json
ops/zk/recursive-proof.json
ops/zk/recursive-verifier.json
ops/zk/recursive-proof.onchain.json
ops/zk/formal-verification-report.json
ops/policy/policy-state.json
ops/policy/healing-actions.json
ops/policy/self-heal-log.json
ops/satellite/offline-attestations.json
ops/satellite/sync-log.json
ops/zkml/model-proof.json
ops/zkml/policy-update.json
ops/zkml/learning-log.json
ops/governance/enforcement-log.json
ops/governance/ai-policy-proposal.json
ops/governance/chain-policy-*.json
ops/security/vault-auth.env

# ghost-helper-bots artifacts (generated)
ghost-helper-bots/reports/
ghost-helper-bots/evidence/
ghost-helper-bots/sbom/
ghost-helper-bots/attestations/

# audit snapshot preflight env overrides (generated)
.audit/snapshots/**/ops/preflight/**/stack.env.override
*.swp
*.swo
*.tmp
*.bak
*~

# temp scripts
tmp-*.js
tmp-*.ts

# local shell env
.envrc
.direnv/

# package manager caches
.pnpm-store/

# secrets / keys / certs
*.pem
*.key
*.crt
*.cer
*.pfx
*.p12
*.jks
*.keystore
*.enc
*.der
*.csr
*.jwt
*.token
*.secret
*.secrets
.vault-token
.npmrc
.npmrc.*
**/.npmrc
**/.npmrc.*
.certs/
.secrets/
secrets/
certs/
.ssh/
.gnupg/
.aws/
.gcloud/
.azure/
**/.certs/
**/.secrets/
**/secrets/
**/certs/
**/.ssh/
**/.gnupg/
**/.aws/
**/.gcloud/
**/.azure/
**/keystore/
**/jwtsecret
**/jwtsecret.*
**/nodekey
**/nodekey.*
**/keys/**/key
**/keys/**/key.pub
.netrc
.pypirc
.docker/
infra/k8s/**/api-secret.yaml

# engine API JWT secrets (local-only)
**/config/jwt.txt
!**/config/jwt.txt.example

# opstack local keystore/password material (local-only)
infra/opstack/config/l1-keystore/
infra/opstack/config/l1-keystore-password
infra/opstack/config/l1-keystore-geth/password.txt

# ghostchain local geth account password (local-only)
infra/ghostchain/geth/password.txt

# backups (often contain envs/keys)
backups/
infra/docker/_backup/
infra/docker/runtime/
infra/docker/audit/
infra/opstack/backups-*/
docs/rollback/
tools/ghostcontrol/secrets/

# terraform state (contains secrets)
.terraform/
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json

# databases / local stores
*.sqlite
*.sqlite3
*.db
*.db-wal
*.db-shm
*.rdb
dump.rdb
appendonly.aof

# security scan outputs
license-report.json
contracts/license-report.json
sbom.spdx.json

# opstack local geth logs
infra/opstack/data/l2-geth-901/geth/nodes/LOG

# ── Sensitive data — credentials, keys, wallets ───────────────────────────────

# Private key / mnemonic / seed material
*.privkey
*.private-key
*.private_key
*.mnemonic
*.mnemonic.txt
*.seed
*.seed.txt
*.seedphrase
*.seed-phrase
*.hdwallet
*.hdwallet.json
mnemonic.txt
seed.txt
seedphrase.txt

# Ghost SDK keystore exports (GhostKeyStore.export())
ghost-keystore.json
*.keystore.json
**/ghost-keystore*.json
**/keystore-export*.json

# API keys / access tokens
*.apikey
*.api-key
*.api_key
api-keys.json
api-keys.yaml
api-keys.toml
*-credentials.json
*-credentials.yaml
credentials.json
credentials.yaml
service-account*.json

# HMAC / signing secrets
*.hmac
*.hmac-secret
*.signing-key
*.signing_key
control-plane-hmac.txt
hmac-secret.txt

# Wallet / signer config files
wallet.json
wallets.json
signer.json
signers.json
**/wallet-config.json
**/signer-config.json

# Ghost hardware wallet transport configs
**/ledger-config.json
**/trezor-config.json

# SSH / GPG runtime material
id_rsa
id_ed25519
id_ecdsa
id_dsa
*.pub
known_hosts

# Cloud provider keys
gcp-service-account.json
gcloud-key.json
aws-credentials
azure-credentials
*.boto
boto.cfg

# Vault / KMS material
vault-unseal*.txt
vault-root-token*.txt
kms-key*.json
*.vault-pass
ansible-vault-password.txt

# Docker registry auth
docker-auth.json
.dockerconfigjson

# CI/CD secrets snapshots
.github-token
.gh-token
ci-secrets*.json

# ── Ghost X Exchange ──────────────────────────────────────────────────────────
# Auto-generated by scripts/deploy-ghostx.sh – contains live contract addresses
.env.ghostx
# Runtime env files (never track; use *.example counterparts)
apps/ghostx/.env
services/ghostx-api/.env
# Next.js build output
apps/ghostx/.next/
# Forge / Hardhat deploy artefacts for Ghost X
contracts/deployments/ghostx-*.json
contracts/broadcast/DeployGhostX.s.sol/
# Docker runtime compose overrides with secrets
docker-compose.ghostx.override.yml

# ghostcain-contracts integration
contracts/ghostcain/node_modules/
contracts/ghostcain/out-codex/
contracts/ghostcain/out-legacy/
contracts/ghostcain/cache-codex/
contracts/ghostcain/cache-legacy/
contracts/ghostcain/.hardhat-cache/
contracts/ghostcain/artifacts/
contracts/ghostcain/typechain-types/

# ── GhostBrain AI OS — runtime artefacts ─────────────────────────────────────
# Memory journals written at runtime (disk-backed AI memory)
services/ghostbrain-core/memory/
services/ghostbrain-core/data/
services/**/memory/*.ndjson
services/**/memory/warm/
services/**/memory/cold/
services/**/memory/archive/
# Predictive model checkpoints (ephemeral, regenerated from live metrics)
services/**/predictive/checkpoints/
# Prometheus push-gateway local state
services/**/pushgateway-state.json

# ── GhostBrain microservices — generated / runtime files ─────────────────────
services/ghostbrain-agent/dist/
services/ghostbrain-cluster/dist/
services/ghostbrain-infra/dist/
services/ghostbrain-memory/dist/
services/ghostbrain-swarm/dist/

# ── Infra installer scripts — generated keys/certs ───────────────────────────
infra/genesis/validator-keys/
infra/genesis/node-keys/
infra/genesis/gentxs/
infra/genesis/*.key
infra/genesis/*.pem
infra/genesis/*.cert

# ── Node / TypeScript build output (workspace-wide) ──────────────────────────
**/dist/
**/build/
**/.tsbuildinfo
**/tsconfig.tsbuildinfo

# ── Test / coverage output ────────────────────────────────────────────────────
**/coverage/
**/.nyc_output/
**/junit.xml
**/*.lcov

# ── OS / editor noise ─────────────────────────────────────────────────────────
Thumbs.db
ehthumbs.db
**/*.swp
**/*.swo
**/*~

# ── Logs ─────────────────────────────────────────────────────────────────────
logs/
**/*.log
**/npm-debug.log*
**/yarn-debug.log*
**/yarn-error.log*
# ── Python artefacts (ai/, infra/hypervisor/supervisor/, etc.) ──────────────
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd
**/*.pyw
**/.python-version
**/venv/
**/.venv/
**/.virtualenv/
**/env/
**/*.egg-info/
**/*.egg
**/.eggs/
**/.mypy_cache/
**/.ruff_cache/
**/.pytest_cache/

# ── GhostBrain Core — runtime artefacts (source code is tracked) ────────────
ghost-brain-core/**/memory/*.ndjson
ghost-brain-core/**/memory/warm/
ghost-brain-core/**/memory/cold/
ghost-brain-core/**/memory/archive/
ghost-brain-core/**/data/
ghost-brain-core/**/checkpoints/
ghost-brain-core/**/*.bin
ghost-brain-core/**/*.safetensors

# ── AI swarm — runtime / model artefacts ────────────────────────────────────
ai/**/data/
ai/**/checkpoints/
ai/**/logs/
ai/**/*.pkl
ai/**/*.joblib

# ── Autonomous installer — runtime state ────────────────────────────────────
autonomous-installer/**/state/
autonomous-installer/**/snapshots/
autonomous-installer/**/tmp/

# ── Economic AI — runtime artefacts ─────────────────────────────────────────
economic-ai/**/predictions/cache/
economic-ai/**/snapshots/

# ── Governance AI — local deploy artefacts ──────────────────────────────────
governance-ai/artifacts/
governance-ai/cache/

# ── Interchain bridge — local deploy artefacts ──────────────────────────────
interchain-bridge/artifacts/
interchain-bridge/cache/

# ── Infrastructure supervisor — runtime state ────────────────────────────────
infrastructure/supervisor/logs/
infrastructure/supervisor/state/
infra/hypervisor/supervisor/logs/
infra/hypervisor/supervisor/state/
infra/hypervisor/supervisor/*.pid

# ── Genesis / installer — generated keys ────────────────────────────────────
genesis-installer/**/*.key
genesis-installer/**/*.pem
genesis-installer/**/*.cert
genesis-installer/**/state/

# ── GID — local identity state ──────────────────────────────────────────────
gid/data/
gid/state/

# ── Validator AI — runtime checkpoints ──────────────────────────────────────
validator-ai/**/checkpoints/
validator-ai/**/data/

# ── Docker supervisor override ───────────────────────────────────────────────

# ── GhostBrain Core — runtime artefacts ─────────────────────────────────────
services/ghostbrain-core/dist/
services/ghostbrain-core/*.pid
services/ghostbrain-core/memory/
services/ghostbrain-core/logs/
docker-compose.supervisor.override.yml
# Generated key bundles — never commit
ghostchain-keys.txt
services/ai-vault/ghostchain-secrets.json

# ════════════════════════════════════════════════════════════════════════════
# SENSITIVE DATA — COMPREHENSIVE EXCLUSIONS
# Generated by full-stack audit 2026-03-11
# ════════════════════════════════════════════════════════════════════════════

# ── Generated key & secret bundles ──────────────────────────────────────────
ghostchain-keys.txt
ghostchain-keys-*.txt
*-keys.txt
*-keys-*.txt
tools/generate_ghostchain_keys.py

# ── Private key files (all formats) ──────────────────────────────────────────
*.key
!*.key.example
!*.key.sample
*.priv
*.privkey
boot.key
nodekey
**/nodekey
**/boot.key
**/node1.key
**/node2.key
**/sequencer.key
**/batcher.key
**/proposer.key
**/challenger.key

# ── TLS / X.509 certificates & keystores ─────────────────────────────────────
*.pem
*.crt
*.cer
*.pfx
*.p12
*.jks
*.keystore
*.der
*.csr
*.certs/
.certs/
certs/
**/.certs/
**/certs/

# ── Wallet / keystore / mnemonic files ───────────────────────────────────────
wallet.json
keystore.json
**/keystore/
**/keystores/
mnemonic.txt
mnemonic*.txt
seed.txt
seed*.txt
*.mnemonic
*.seed

# ── JWT / token / secret files ────────────────────────────────────────────────
*.jwt
jutsecret
**/jwtsecret
**/jwtsecret.*
*.token
*.secret
*.secrets
.secrets/
secrets/
**/.secrets/
**/secrets/
*-secrets.json
*secrets*.json
services/ai-vault/ghostchain-secrets.json

# ── Environment variable files (real values, not examples) ───────────────────
/.env
**/.env
.env.local
**/.env.local
.env.*.local
**/.env.*.local
.env.production
**/.env.production
.env.prod
**/.env.prod
.env.mainnet
**/.env.mainnet
.env.testnet
**/.env.testnet
.env.staging
**/.env.staging
.env.override
**/.env.override
stack.env
/stack.env
**/stack.env
services/stack.env
services/.env
infra/opstack/.env
infra/opstack/.env.secrets
infra/ghostchain/.env
infra/ghostchain/.env.l1
infra/gns/.env
environments/**/*.env
!environments/**/*.env.example
!environments/**/*.env.sample
!environments/**/*.env.template
infra/hypervisor/**/*.env
!infra/hypervisor/**/*.env.example
!infra/hypervisor/**/*.env.sample
infra/docker/compose/*.env
infra/docker/compose/**/*.env
!infra/docker/compose/*.env.example
!infra/docker/compose/**/*.env.example
observability/**/.env
observability/**/stack.env
services/ghost-compliance/.env
services/ghost-compliance-worker/.env
services/ghost-guard/.env
services/ghost-relayer/.env
services/ghost-rollup-proposer/.env.l2
services/ghost-rollup-proposer/.env.l3
services/ghost-rollup-challenger/.env.l2
services/ghost-rollup-challenger/.env.l3
apps/ghost-ui/.env
apps/api/.env
apps/web/.env.local

# ── Vault rendered secrets & tokens ──────────────────────────────────────────
infra/ghostchain/secrets/
infra/opstack/secrets/
infra/opstack/l3/secrets/
infra/docker/secrets/
.vault-token
**/.vault-token
vault-token*
evidence/phase2/vault-approle-bootstrap.json
evidence/phase2/vault-approle-rotation.json
evidence/phase2/vault-*.json
evidence/**/vault-*.json
# ai-vault session & secrets
services/ai-vault/ghostchain-secrets.json

# ── AppRole / credential artefacts ───────────────────────────────────────────
approle*.json
*-approle*.json
vault-approle-*.json
*.creds
*.credentials
*credentials*.json
google-credentials*.json
service-account*.json
firebase*-adminsdk*.json
.aws/
.gcloud/
.azure/
**/.aws/
**/.gcloud/
**/.azure/

# ── SSH & GPG keys ────────────────────────────────────────────────────────────
.ssh/
**/.ssh/
.gnupg/
**/.gnupg/
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
id_ecdsa
id_ecdsa.pub
id_dsa
id_dsa.pub
*.ppk

# ── Geth / node password files ────────────────────────────────────────────────
**/password.txt
!**/password.txt.example
!**/password.txt.sample
infra/ghostchain/geth/password.txt
infra/opstack/config/l1-keystore-geth/password.txt
infra/opstack/config/l1-keystore-password

# ── Database files (runtime data) ────────────────────────────────────────────
*.db
*.sqlite
*.sqlite3
*.sql.gz
*.dump
apps/api/data/
apps/api/.sessions/
apps/api/.session-store/

# ── Terraform state & variable files ─────────────────────────────────────────
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
.terraform/
.terraform.lock.hcl
infra/terraform/**/*.tfvars
!infra/terraform/**/*.tfvars.example
!infra/terraform/**/*.tfvars.sample
# Generated terraform plan output
tfplan
*.tfplan

# ── Kubernetes secrets & kubeconfig ──────────────────────────────────────────
kubeconfig
*.kubeconfig
kube-config*
~/.kube/config
infra/k8s/base/api-secret.yaml
infra/k8s/secrets/
infra/helm/**/values.secret.yaml
infra/helm/**/values.prod.yaml
infra/helm/**/values.mainnet.yaml

# ── CI / platform credentials ─────────────────────────────────────────────────
.npmrc
.npmrc.*
**/.npmrc
**/.npmrc.*
.sentryclirc
.netrc
.htpasswd
auth.env
authinfo
*.wif

# ── P2P discovery files (contain node IDs seeded from keys) ──────────────────
infra/ghostchain/config/bootnode-enode.txt
infra/ghostchain/ibft/networkFiles/peers.txt

# ── IBFT / consensus generated config (may contain validator keys) ────────────
infra/ghostchain/ibft/config.generated.json
infra/ghostchain/ibft/config.generated.yaml
infra/ghostchain/ibft/networkFiles/genesis.json
**/config.generated.json
**/config.generated.yaml

# ── Ops security vault auth env ──────────────────────────────────────────────
ops/security/vault-auth.env
ops/security/trivy-fs.json

# ── Local shell environment hooks ────────────────────────────────────────────
.envrc
.direnv/
env.sh

# ── Scratch / temporary sensitive files ──────────────────────────────────────
*.bak.env
pk.txt
private_key*.txt
deployment-keys*.txt
*.enc

# ── AI Vault runtime artifacts ────────────────────────────────────────────────
# Encrypted SQLite vault databases (contain live secrets — never commit)
services/ai-vault/data/
services/ai-vault/*.db
services/ai-vault/**/*.db
# Key material export files (emergency recovery — store offline)
services/ai-vault/exports/
services/ai-vault/backups/
# Vault cluster node identity keys (generated on first boot)
services/ai-vault/deploy/node-keys/
# Lock files from package managers inside the service
services/ai-vault/package-lock.json

# ════════════════════════════════════════════════════════════════════════════
# MODERN TOOLING — added 2026-03-13
# ════════════════════════════════════════════════════════════════════════════

# ── Playwright testing ────────────────────────────────────────────────────────
playwright-report/
**/playwright-report/
test-results/
**/test-results/
.playwright/

# ── Vitest ────────────────────────────────────────────────────────────────────
**/.vitest-cache/
**/vitest-coverage/

# ── Storybook ─────────────────────────────────────────────────────────────────
storybook-static/
**/storybook-static/
.storybook/cache/

# ── Biome (formatter/linter) ──────────────────────────────────────────────────
.biome/
**/.biome/
biome.json.lock

# ── Local GitHub Actions runner (act) ────────────────────────────────────────
.act/
**/.act/

# ── Vercel / serverless deployment artefacts ─────────────────────────────────
.vercel/
**/.vercel/
.wrangler/
**/.wrangler/

# ── Hardhat network fork cache ────────────────────────────────────────────────
hardhat-network-fork-cache/
**/hardhat-network-fork-cache/

# ── npm packed tarballs ───────────────────────────────────────────────────────
*.tgz
!contracts/lib/**/*.tgz

# ── Docker Compose local overrides (contain host-specific paths / secrets) ───
docker-compose.override.yml
docker-compose.*.override.yml
**/docker-compose.override.yml

# ── Cursor AI IDE workspace state ─────────────────────────────────────────────
.cursor/cache/
.cursor/logs/
.cursor/tmp/
# .cursor/rules is intentionally NOT ignored — tracked for AI context

# ── AI / ML model weight artefacts (broader) ─────────────────────────────────
**/*.gguf
**/*.ggml
**/*.onnx
**/*.ort
**/*.pt
**/*.pth
**/*.h5
**/*.hdf5
**/*.msgpack
# .bin and .safetensors already covered per-service above; catch remainder:
services/**/*.safetensors
services/**/*.bin
# Job library serialisations
**/*.joblib
**/*.pkl

# ── Foundry local profile overrides ──────────────────────────────────────────
contracts/foundry-local.toml
contracts/.foundry/

# ── Cargo / Rust (used by some infra tools) ───────────────────────────────────
**/target/

# ── Go build cache (ghostchaind, op-node helpers) ────────────────────────────
**/.gomodcache/
**/vendor/
**/go.sum.bak

# ── Sentry CLI ────────────────────────────────────────────────────────────────
.sentry/
**/.sentry/

# ── Turbo build manifest (generated, not source) ─────────────────────────────
.turbo/
**/.turbo/

# ── Oxlint / ESLint caches ────────────────────────────────────────────────────
.oxc-cache/
**/.oxc-cache/

# ── GhostChain devnet runtime snapshots (local only) ─────────────────────────
devnet-snapshots/
**/devnet-snapshots/
.devnet/

# ── Jupyter / IPython (used by economic-ai / analytics) ──────────────────────
**/.ipynb_checkpoints/
*.ipynb.bak

# ── SBOM artefacts (generated by CI, not source) ─────────────────────────────
*.spdx.json
*.spdx
*.cdx.json
*.bom.json
**/sbom/
contracts/sbom.spdx.json

# ── GSX / GSN / GCM / GSR / GWF — Sovereign Financial Infrastructure ─
# Rust engine build artifacts (also covered by **/target/ above, explicit for clarity)
services/gsx-engine/target/
services/gsx-engine/.cargo/

# Runtime data volumes — never track mounted DB / chain data
services/gsx-*/data/
services/gsn-*/data/
services/gcm-*/data/
services/gsr-*/data/
services/gwf-*/data/

# PostgreSQL / TimescaleDB local data dirs
**/pgdata/
**/postgres-data/

# Redis dump files
**/dump.rdb
**/*.rdb

# Compiled Solidity artifacts (forge / hardhat output) for new GSX contract suites
contracts/src/gsx/out/
contracts/src/gsx/cache/
contracts/src/gsn/out/
contracts/src/gsn/cache/
contracts/src/gcm/out/
contracts/src/gcm/cache/
contracts/src/gsr/out/
contracts/src/gsr/cache/
contracts/src/gwf/out/
contracts/src/gwf/cache/

# HSM / MPC key material (should NEVER be committed)
**/*.hsm
**/*.p12
**/*.pfx
**/*.key
**/keystore/
**/mpc-keys/
**/sovereign-keys/

# Infra generated configs with secrets
infra/gsx/k8s/**/*.secret.yaml
infra/gsx/terraform/**/*.tfvars
infra/gsx/terraform/.terraform/
infra/gsx/terraform/*.tfstate
infra/gsx/terraform/*.tfstate.backup

# AI model weights for gsx risk-ai / gwf economic-ai (local training runs)
services/gsx-*/models/*.bin
services/gwf-*/models/*.bin
services/gsr-*/models/*.bin

# Stray IDE / AI chat artifacts
"packages/ghost-sdk/src/runtime/## GitHub Copilot Chat.litcoffee"
**/"## GitHub Copilot Chat"*
**/"## GitHub Copilot"*

# ── GSE — Sovereign Economy Engine ────────────────────────────────────────────
services/gse-*/data/
services/gse-*/pgdata/
contracts/src/gse/out/
contracts/src/gse/cache/
infra/gse/terraform/*.tfstate
infra/gse/terraform/*.tfstate.backup
infra/gse/terraform/.terraform/
infra/gse/k8s/**/*.secret.yaml

# GSI — Ghost Sovereign Identity Network
services/gsi-*/data/
services/gsi-*/node_modules/
services/gsi-*/dist/
contracts/src/gsi/out/
contracts/src/gsi/cache/
services/gsi-*/secrets/
**/biometric-data/
**/biometric-raw/
**/*.biometric
infra/gsi/terraform/.terraform/
infra/gsi/terraform/*.tfstate
infra/gsi/terraform/*.tfvars
infra/gsi/k8s/**/*.secret.yaml

# GSA — Ghost Sovereign AI Network
services/gsa-*/data/
services/gsa-*/node_modules/
services/gsa-*/dist/
contracts/src/gsa/out/
contracts/src/gsa/cache/
services/gsa-*/model-weights/
**/ai-models/
**/*.onnx
**/*.pkl
**/*.pt
infra/gsa/terraform/.terraform/
infra/gsa/terraform/*.tfstate
infra/gsa/terraform/*.tfvars
infra/gsa/k8s/**/*.secret.yaml

# ghostvm-ai runtime plans and discovery state (ephemeral, timestamped)
services/ghostvm-ai/plans/
services/ghostvm-ai/state/

# ── CodeQL / SARIF security scan output ──────────────────────────────────────
*.sarif
*.sarif.gz
**/*.sarif
codeql-results/
.codeql/

# ── CI-generated report files (produced by workflow jobs, not source) ─────────
sed-engine-report.json
brand-audit-results.json
brand-enforcer-results.json
contract-brand-results.json
nightly-security-report.json
trivy-image.json
supply-chain-security-artifacts/
digest-refresh-report.json
digest-refresh-report.md
digest-rollforward-candidates.md

# ── GitHub Actions local runner (act) ────────────────────────────────────────
.act/

# ── VS Code copilot chat workspace storage (local cache, not source) ──────────
.vscode-server/
**/.vscode-server/

# ── Node.js 22+ runtime diagnostics ─────────────────────────────────────────
node_modules/.cache/
*.heapsnapshot
*.cpuprofile
*.heapprofile
isolate-*.log

# ── Package manager lock files for auto-installed service deps ────────────────
# (only for generated lock files in services that are not source-of-truth)
services/ai-adoption/package-lock.json
services/ai-expansion/package-lock.json
services/ai-governance/package-lock.json
services/ai-growth/package-lock.json
services/ai-hypervisor/package-lock.json
services/ai-interchain/package-lock.json
services/ai-marketing/package-lock.json

# ── Forge / Foundry gas reports (generated output) ───────────────────────────
contracts/gas-report.txt
**/gas-report.txt

# ── Local smoke env file (written by release-smoke workflow) ─────────────────
.smoke.env

# ── Release attestation keys (ephemeral, CLI-generated) ──────────────────────
/tmp/release-attestation.key
