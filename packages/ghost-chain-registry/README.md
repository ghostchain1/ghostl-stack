# @ghostchain/ghost-chain-registry

Canonical chain identity and routing metadata for:

- `ghostchain`
- `ghostl2`
- `ghostl3`

This package is the source of truth for chain IDs, RPC URLs, explorers,
ports, proof mode, and canonical contract addresses across the GhostStack
workspace.
