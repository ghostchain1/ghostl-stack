{
  "name": "hardhat-ghost",
  "version": "1.0.0",
  "type": "module",
  "description": "Hardhat plugin — injects the Ghost SDK namespace into the Hardhat Runtime Environment",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "hardhat": "^2.0.0",
    "@ghostchain/sdk": "*",
    "@ghostchain/ghost-contract-factory": "*"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "hardhat": "^3.2.0",
    "@ghostchain/sdk": "*",
    "@ghostchain/ghost-contract-factory": "*"
  },
  "peerDependenciesMeta": {
    "@ghostchain/ghost-contract-factory": {
      "optional": true
    }
  },
  "keywords": ["hardhat", "ghost", "ghostchain", "gst", "plugin"],
  "license": "MIT"
}
